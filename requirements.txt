# Pinned deliberately (spec §3): the Play parser breaks when Google changes page layout,
# so an unpinned bump is an unannounced production change. Verified working 2026-08-17
# on Python 3.12.4.
google-play-scraper==1.2.7
requests==2.34.2
psycopg[binary]==3.2.10
python-dotenv==1.1.1
